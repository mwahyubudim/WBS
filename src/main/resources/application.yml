spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/wbs
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      '[hibernate.dialect]': org.hibernate.dialect.MySQL8Dialect
